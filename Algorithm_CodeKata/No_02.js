// 문제 2 - 두 수의 곱

// raw code
function solution(num1, num2) {
    var answer = 0;
    return answer;
}

// 해결 코드
function solution_solved(num1, num2) {
    var sum = num1 * num2;
    var answer = sum;
    return answer;
}
